# the-roac-backend

## Setup
1. Clone repo
2. Copy `.env.example` -> `.env` and set values
3. Install
   npm install
4. Create DB in Postgres
5. Run migrations:
   npx sequelize db:migrate
6. Start in dev:
   npm run dev
